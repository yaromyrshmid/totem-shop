import React from "react"
import StripeCheckout from "gatsby-plugin-stripe"

const StripeButton = ({ price }) => {
  const priceForStripe = price * 100
  return <div></div>
}

export default StripeButton
